# EdgeBase release / production environment variables
# Copy to .env.release before deploy or self-hosting.
# Deploy syncs this file to Cloudflare Secrets.

# Required to preserve auth/admin tokens across re-deploys
JWT_USER_SECRET=
JWT_ADMIN_SECRET=

# Optional for Docker/self-hosting or if you want to pin a fixed service key.
# Cloudflare deploy can auto-manage SERVICE_KEY, so leave this commented unless needed.
# SERVICE_KEY=

# Optional PostgreSQL connection strings
# DB_POSTGRES_APP_URL=postgres://user:pass@host/db?sslmode=require
# AUTH_POSTGRES_URL=postgres://user:pass@host/db?sslmode=require

# Optional email provider settings
# EDGEBASE_EMAIL_PROVIDER=resend
# EDGEBASE_EMAIL_API_KEY=
# EDGEBASE_EMAIL_FROM=noreply@edgebase.fun

# Optional OAuth settings
# EDGEBASE_AUTH_ALLOWED_OAUTH_PROVIDERS=google,github
# EDGEBASE_OAUTH_GOOGLE_CLIENT_ID=
# EDGEBASE_OAUTH_GOOGLE_CLIENT_SECRET=
# EDGEBASE_OAUTH_GITHUB_CLIENT_ID=
# EDGEBASE_OAUTH_GITHUB_CLIENT_SECRET=
