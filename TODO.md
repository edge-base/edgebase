# TODO

- Configure Cloudflare Pages for docs deployment:
  Root directory: repository root
  Build command: `pnpm install --frozen-lockfile && pnpm --filter docs build`
  Build output directory: `docs/build`
  Framework preset: `None`
